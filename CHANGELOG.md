2026-04-19  Ryan Musante

- v3.1: 5 `retroarch.cfg` value changes + README refactor to max-performance baseline. **Modifies `retroarch.cfg`:** `video_shader_enable = "true"` → `"false"` (HIGH; disables global CRT shader pipeline entirely for maximum GPU headroom at 4K60; preset path `../shaders/shaders_slang/crt/crt-easymode.slangp` preserved but inert; user opt-in via in-app toggle or direct edit to re-enable CRT aesthetic). `fastforward_ratio = "4.0"` → `"0.0"` (MED; uncaps fast-forward for Tier 1 cores; Tier 2 self-limits ~1× real-time on the no-JIT interpreter stack, so no CPU risk; if Metal-on-tvOS FF fails to engage, revert to "4.0" per §7 note). `autosave_interval = "60"` → `"300"` (MED; reduces SRAM flush frequency from every 60 s to every 5 min = 5× fewer volatile-cache writes; `save_file_compression = "true"` retained to keep each write small; widens data-loss window to max 5 min on tvOS force-kill). `audio_resampler_quality = "2"` → `"1"` (LOW; drops from sinc-lower to sinc-lowest; two below upstream default 3; minimal CPU reduction on audio path; imperceptible at 48 kHz output). `savestate_thumbnail_enable = "true"` → `"false"` (LOW; skips screenshot + PNG encode on every state save; removes slot preview thumbnails in menu). Key count unchanged at 75. **README updates:** landing paragraph — "conservative baseline" characterization reworked to "max-performance baseline" with v3.1 shader-disable note and opt-in re-enable guidance. §7 Video settings Video Shaders row — Value `ON (video_shader_enable = "true")` → `OFF (video_shader_enable = "false")`; Notes reworded to reflect v3.1 default-off gate and opt-in re-enable path. §7 Video settings Shader Preset row — Notes reworded to flag preset inert while `video_shader_enable = "false"`. §7 Latency reduction Fast Forward Ratio row — Value `4×` → `Uncapped`; Notes reworded to reflect uncapped ceiling with revert-to-4× fallback for Metal/tvOS FF non-engagement. §7 Additional settings Audio Resampler Quality row — Value `2` → `1`; Notes reworded to "Two below upstream 3 (v3.1); minimal CPU; imperceptible at 48 kHz". §7 Additional settings State Thumbnails row — Value `ON` → `OFF`; Notes reworded to "v3.1: disabled to skip screenshot+PNG encode on every state save; no slot previews in menu". §7 Hotkeys Save state behavior callout — `autosave_interval = "60"` → `"300"`; "every 60 seconds" → "every 5 minutes"; added compression-retained clause to justify the interval increase. §8 Shaders — new ⚠️ v3.1 callout at section head documenting default-off state and re-enable paths (edit `retroarch.cfg` or toggle via Quick Menu → Shaders → Video Shaders); remaining guidance preserved for users who flip shaders back on. §11 Setup Checklist Verify after relaunch — "Global shader loads automatically (`crt-easymode`) on every core. Verify via Quick Menu → Shaders" rewritten to "Shader pipeline disabled by default (`video_shader_enable = "false"`, v3.1). If CRT aesthetic is desired, flip to `"true"` and verify global `crt-easymode` loads via Quick Menu → Shaders". All other 70 `retroarch.cfg` keys and values unchanged from v3.0. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.92 block removed). Version badge 3.0 → 3.1. Companion `retroarch-configs` bumped to v3.1 (5 `.opt` files modified: `Genesis Plus GX.opt` -1 key (`genesis_plus_gx_lowpass_range = "55"` removed as inert after `audio_filter` low-pass → off), `mGBA.opt` 2 value changes (`mgba_interframe_blending` mix_smart → disabled, `mgba_audio_low_pass_filter` enabled → disabled), `PCSX-ReARMed.opt` +1 key (`pcsx_rearmed_frameskip_threshold = "33"` added, `pcsx_rearmed_frameskip_type` disabled → auto_threshold), `Beetle PCE Fast.opt` `pce_fast_cdspeed` 2 → 4, `Mesen.opt` `mesen_reduce_dmc_popping` enabled → disabled; opt key count net unchanged at 27 (Genesis -1 + PCSX +1); cfg key count unchanged at 49).

2026-04-19  Ryan Musante

- v3.0: MAJOR version-sync release. Establishes lockstep versioning across companion repositories: both `retroarch-appletv4k` and `retroarch-configs` now share a single MAJOR.MINOR tag and will bump together on every future release regardless of which side contains the real file changes. Pre-sync state: `retroarch-appletv4k` at v2.95, `retroarch-configs` at v1.57 (the two repos had evolved independently since inception with asymmetric version numbering). No `retroarch.cfg` changes, no `.cfg`/`.opt` drift, no behavioral differences from v2.95 / companion v1.57 — file contents byte-identical to the v2.95 shipping state. **README updates:** §13 Versioning — policy text revised to document the lockstep convention explicitly. Prior text: "This repository uses `vMAJOR.MINOR` (no patch component). `MAJOR` increments on incompatible structural changes (filesystem layout, breaking config schema, removed features)." New text prepends a lockstep-scope sentence and expands the MAJOR clause to include cross-repo version-sync events: "This repository uses `vMAJOR.MINOR` (no patch component) in lockstep with its companion repository — both `retroarch-appletv4k` and `retroarch-configs` share a single MAJOR.MINOR tag that increments together on every release, regardless of which side contains the real file changes. `MAJOR` increments on incompatible structural changes (filesystem layout, breaking config schema, removed features) or cross-repo version-sync events (e.g. v3.0 re-aligned the two repos after they had evolved independently at v2.95 / v1.57)." MINOR clause, CHANGELOG GNU-style-date convention, and 5-entry trim policy unchanged. Global `retroarch.cfg` key count unchanged at 75. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.91 block removed). Version badge 2.95 → 3.0. Companion `retroarch-configs` bumped to v3.0 (matching sync; §11 Versioning text revised identically to §13; file contents byte-identical to v1.57 shipping state).

2026-04-19  Ryan Musante

- v2.95: SYNC — no `retroarch.cfg` changes, no README table changes (badge only). Version tag aligns the companion release channel with `retroarch-configs` v1.57, which (a) removes the Mupen64Plus-Next stutter-mitigation tax introduced in v1.56 (per-core `audio_latency = "64"` and `video_max_swapchain_images = "3"` pins dropped from `Mupen64Plus-Next.cfg`; Mupen now inherits the global low-latency posture of `audio_latency = 48` + `video_max_swapchain_images = 2` that v2.94 restored for all Tier 1 cores), and (b) applies two max-performance tunes to Beetle PCE Fast: `Beetle PCE Fast.opt` `pce_fast_cdimagecache = "disabled"` → `"enabled"` (full CD image precache to RAM at startup; eliminates disc seek latency during gameplay; aligns with the PCSX-ReARMed `cd_readahead = "333000"` precache philosophy), and `Beetle PCE Fast.cfg` `run_ahead_frames = "1"` → `"2"` (brings PCE Fast to parity with the other 6 Tier 1 cores; prior 1-frame cap justified as "CDROM seek determinism" was over-conservative — the real constraint is Run Ahead Secondary Instance, which PCE Fast already pins to `false` per upstream libretro/beetle-pce-fast-libretro issue #127 confirming 2-frame single-instance run-ahead works on CD games). Global `retroarch.cfg` key count unchanged at 75. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.90 block removed). Version badge 2.94 → 2.95. Companion `retroarch-configs` bumped to v1.57 (`Mupen64Plus-Next.cfg` -2 keys; `Beetle PCE Fast.opt` cdimagecache value swap; `Beetle PCE Fast.cfg` run_ahead_frames value swap; cfg key count 51 → 49, opt key count unchanged at 27).

2026-04-19  Ryan Musante

- v2.94: 3 `retroarch.cfg` value reverts to v2.92 baseline + thermal-language cleanup across §7. **Reverts v2.93 globals:** `audio_latency = "96"` → `"48"`. `audio_rate_control_delta = "0.012"` → `"0.008"`. `video_max_swapchain_images = "3"` → `"2"`. Rationale: v2.93 widened the global audio buffer and swap chain to mitigate N64 Mupen64Plus-Next stutter on the Angrylion software RDP stack, but that penalized all 7 Tier 1 cores + PCSX-ReARMed with ~16 ms of output latency and doubled audio buffer depth they don't need. v2.94 localizes stutter mitigation to the one core that needs it: companion `retroarch-configs` v1.56 adds per-core `audio_latency = "64"` and `video_max_swapchain_images = "3"` pins to `Mupen64Plus-Next.cfg`, plus flips `mupen64plus-angrylion-multithread` to the upstream-default `"all threads"` for maximum RDP parallelism. Global retroarch.cfg audio/video knobs return to minimum-viable-latency posture for Tier 1. Key count unchanged at 75. **Thermal-language cleanup (cosmetic; no behavior change):** ATV4K is plugged-in and passively cooled, but the emulation workload is steady-state, not bursty — thermal throttling essentially never fires. Real constraint is CPU budget / real-time deadline. §7 Latency reduction Fast Forward Ratio row Notes — "A15 has thermal headroom for 8/16-bit cores at 4K60; Tier 2 (Mupen/PCSX) already thermal-limits itself" → "A15 has CPU/GPU headroom for 8/16-bit cores at 4K60; Tier 2 (Mupen/PCSX) are already CPU-bound at ~1× real-time on the interpreter-only stack and self-limit below the cap". §7 Latency reduction Run-Ahead Mode row Notes — "A15 thermal-safe" → "fits within A15 CPU budget". §7 Additional settings Pause on Menu row Notes — "thermal relief for A15" → "CPU/GPU relief for A15". **README table updates for the cfg reverts:** §7 Video settings Max Swapchain Images row — Value `3` → `2`; Notes reworded to "Double-buffer; minimum output latency on fixed-refresh tvOS. Mupen64Plus-Next pins `3` per-core for pacing slack on the CPU-bound Angrylion stack". §7 Additional settings Audio Latency row — Value `96 ms` → `48 ms`; Notes reworded to "Minimum viable buffer on tvOS coreaudio for Tier 1 cores; Mupen64Plus-Next pins `64` per-core for CPU-underrun tolerance on the interpreter-only N64 stack; PCSX-ReARMed pins `48` as drift-guard". §7 Additional settings Rate Control Delta row — Value `0.012` → `0.008`; Notes reworded to "DRC headroom from upstream 0.005; handles tvOS clock drift without audible pitch wobble". All other 72 `retroarch.cfg` keys and values unchanged from v2.93. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.89 block removed). Version badge 2.93 → 2.94. Companion `retroarch-configs` bumped to v1.56 (`Mupen64Plus-Next.cfg` +2 keys: `audio_latency = "64"`, `video_max_swapchain_images = "3"`; `Mupen64Plus-Next.opt`: `mupen64plus-angrylion-multithread "3"` → `"all threads"` upstream default; cfg key count 49 → 51; opt key count unchanged at 27).

2026-04-19  Ryan Musante

- v2.93: 3 `retroarch.cfg` value changes targeting N64 Mupen64Plus-Next audio stutter on the Angrylion software RDP stack. **Modifies:** `audio_latency = "48"` → `audio_latency = "96"` (doubles audio buffer depth from 48 ms to 96 ms; passively-cooled A15 on tvOS cannot JIT, so cached interpreter + CXD4 LLE RSP + Angrylion LLE RDP transiently starves the audio callback under heavy scenes; wider buffer absorbs the underruns at the cost of ~48 ms additional input→audio latency). `audio_rate_control_delta = "0.008"` → `audio_rate_control_delta = "0.012"` (widens DRC resample window from ±0.8% to ±1.2%; lets the resampler stretch/compress more aggressively to bridge frame-pacing jitter from Tier 2 cores without audible pitch wobble; still well below the upstream maximum). `video_max_swapchain_images = "2"` → `video_max_swapchain_images = "3"` (activates the documented v2.92 fallback path: triple-buffer swap chain; adds one frame of pacing slack so CPU-bound cores that miss a vsync deadline drop a frame instead of glitching mid-scanout; trade is ~16 ms additional output latency). Key count unchanged at 75. `README.md` §7 Video settings Max Swapchain Images row — Value column `2` → `3`; Notes reworded from "Double-buffer; lower latency than triple on fixed-refresh tvOS; revert to `3` if pacing artifacts" to "Triple-buffer; +~16 ms latency vs 2 in exchange for one frame of pacing slack; stabilizes CPU-bound Tier 2 cores (Mupen64Plus-Next Angrylion stack). Revert to `2` if latency matters more than stutter on Tier 1". §7 Additional settings Audio Latency row — Value column `48 ms` → `96 ms`; Notes reworded from "From 64 ms baseline; raise per-core if crackling" to "From 48 ms (v2.92); doubled buffer for CPU-underrun tolerance on interpreter-only Tier 2 cores (Mupen, PCSX)". §7 Additional settings Rate Control Delta row — Value column `0.008` → `0.012`; Notes reworded from "DRC headroom from upstream 0.005; handles tvOS clock drift" to "DRC headroom widened to ±1.2% (from 0.008); absorbs Tier 2 frame-pacing jitter without pitch wobble". All other 72 `retroarch.cfg` keys and values unchanged from v2.92. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.88 block removed). Version badge 2.92 → 2.93. Companion `retroarch-configs` bumped to v1.55 (`Mupen64Plus-Next.opt` `mupen64plus-angrylion-multithread` 2 → 3; opt key count unchanged at 27; cfg key count unchanged at 49).

2026-04-18  Ryan Musante

