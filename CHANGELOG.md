2026-04-18  Ryan Musante

- v2.85: Spec conformance pass against `RA-TVOS-CFG-SPEC` (2026-04-18) — 6 requirements applied to `retroarch.cfg`. **CAT-I / CAT-II adds:** `audio_driver = "coreaudio"` (REQ-006; pins driver against silent fallback; `coreaudio3` is master-only per [RA] CHANGES.md and must not be used in v1.22.x stable). `netplay_nat_traversal = "false"` (REQ-005; registration-site default is `true` at [RA] configuration.c:2272 — the `DEFAULT_NETPLAY_NAT_TRAVERSAL=false` macro at config.def.h:1371 is dead code, so without this pin a fresh config performs UPnP/NAT-PMP probes on startup). `input_overlay_enable = "false"` (REQ-007; v1.21.0 changed preferred-overlay auto-load default on mobile-class platforms, triggering an overlay subsystem scan at startup on tvOS where no touch surface exists). **CAT-III adds:** `netplay_use_mitm_server = "false"` (REQ-013; matches existing explicit hardening pattern — six `cloud_sync_*` keys already pinned default-false — guarding against relay-server opt-in on default drift). `netplay_start_as_spectator = "false"` (REQ-014; closes remaining netplay attack-surface keys). **CAT-II remove:** `video_use_metal_arg_buffers = "true"` stripped (REQ-011; key registration guarded by `#if defined(__APPLE__) && defined(HAVE_VULKAN)` at [RA] configuration.c:1970–1972, inert when `video_driver=metal`; re-activates only if video_driver is switched to vulkan). Key count 71 → 75 (−1 + 5). `README.md` §7 Video settings: `Metal Argument Buffers` row removed. §7 Additional settings: added `Audio | Audio Driver` row (above `audio_out_rate`); added `Input | Overlay Subsystem` row (after `Menu Toggle Combo`); added three `Network` rows for `netplay_nat_traversal`, `netplay_use_mitm_server`, `netplay_start_as_spectator` (between existing `Netplay Public Announce` and `Discord RPC`). CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.80 block removed). Version badge 2.84 → 2.85. Companion `retroarch-configs` bumped to v1.47 (spec REQ-001–004, 008–010, 012, 015–017 applied: 11 requirements across 8 `.cfg` and 3 `.opt` files).

2026-04-17  Ryan Musante

- v2.84: Shader architecture reversal — reinstate global `video_shader` in `retroarch.cfg`; companion `retroarch-configs` removes per-core `video_shader` lines. `retroarch.cfg` L15 added: `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"` (single-pass; runs native 4K60 on A15; brighter than crt-easymode; Low GPU cost per §8 table). Selected as best all-rounder across Tier 1 CRT cores and Tier 2 interpreter cores. Key count 70 → 71. `README.md` §7 Video settings: Video Shaders row Notes simplified to pipeline gate only; new `Shader Preset` row added documenting the global `video_shader` path. §8 Applying a shader: rewritten to describe global default + per-core override workflow (reverses v2.79 per-core-only architecture). §8 Handheld note added for mGBA users who prefer LCD aesthetic (apply `handheld/lcd-grid-v2.slangp` via Save Core Preset). §8 tuning paragraph: `crt-easymode 4K parameters` → `crt-aperture 4K parameters` with generic "tune in Shader Parameters menu" guidance (no invented values). §8 L403 Note: stale per-core shader path explanation simplified (no longer references per-core `config/<core_name>/` paths for shaders; still resolves same way as `../ROMs`, `../BIOS`, `../saves`). §11 Setup Checklist shader-verify item: per-core list → global `crt-aperture`. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.79 block removed). Version badge 2.83 → 2.84. Companion `retroarch-configs` bumped to v1.46 (per-core `video_shader` stripped from all 8 `.cfg` files; per-core `.cfg` key counts all decrease by 1).

2026-04-17  Ryan Musante

- v2.83: SYNC — no `retroarch.cfg` changes. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.77 + v2.78 blocks removed). Version badge 2.82 → 2.83. Companion `retroarch-configs` bumped to v1.45: removed dead sentinel `config/.gitkeep` (redundant with 16 tracked `.cfg`/`.opt` files); `README.md` §2 File Structure line updated from "8 `.cfg`, 8 `.opt`, and `config/.gitkeep`" → "8 `.cfg` and 8 `.opt` files, flat under `config/`"; companion CHANGELOG trimmed to last 5 minor versions (v1.39 block removed). Companion shipped ZIP now contains exactly 16 files under `config/` (was 17). No per-core override changes.

2026-04-17  Ryan Musante

- v2.82: `README.md` — documentation-sync pass fixing four README↔`retroarch.cfg` mismatches. (1) §4 Filesystem layout "Saves and savestates" callout (L182): literal-quote fix from `savefile_directory = "config/saves"` / `savestate_directory = "config/states"` → `savefile_directory = "../saves"` / `savestate_directory = "../states"` with inline note that they resolve to `config/saves/` and `config/states/` at runtime — matches actual `retroarch.cfg` L42–43. (2) §7 Additional settings Cloud row (L328): split `cloud_sync_driver` from boolean sub-keys — driver is `""` (empty string, not `"false"`); saves/configs/thumbs/system remain `"false"`; all six keys still set explicitly. (3) §1 intro (L11) + §3 Recommended setup steps 3–4 (L125–126): File Browser and System/BIOS wording changed from "preset to `config/ROMs/`" / "preset to `config/BIOS/`" to literal cfg values `../ROMs` / `../BIOS` with "(resolves to `config/ROMs/`)" / "(resolves to `config/BIOS/`)" clarification — matches the `../` relative-path scheme already documented at §8 L399 for `../shaders`. (4) §7 Additional settings (after Max Users row): added row for `input_menu_toggle_gamepad_combo = "2"` — last undocumented `retroarch.cfg` key; previously surfaced only indirectly via §7 Hotkeys "Open Quick Menu | L3 + R3". All 70 `retroarch.cfg` keys now documented in §7 tables or callouts. No `retroarch.cfg` changes. Version badge 2.81 → 2.82. Companion `retroarch-configs` bumped to v1.44 (SYNC bump; no `.cfg` / `.opt` changes).

2026-04-16  Ryan Musante

- v2.81: `retroarch.cfg` + 16 per-core `.cfg`/`.opt` — strip all `#` comment and blank lines; `retroarch.cfg` 130L → 69L, per-core files collectively 112L → 65L. Keys, values, and key counts unchanged (69 global + 54 per-core). `README.md` §4 Filesystem layout: `shaders/` nested under `config/` with `shaders_slang/{crt,handheld}/` subdirs — prior tree was missing the intermediate `shaders/` parent required by per-core cfg paths `../shaders/shaders_slang/...` which resolve from `config/<core_name>/` up to `config/`, then into `shaders/shaders_slang/` (matches RetroArch native `Save Core Preset` output per libretro docs: shortest relative path). §8 shader upload path `config/shaders_slang/crt/` → `config/shaders/shaders_slang/crt/`. §5 BIOS intro `32-bit+ systems` → `CD-based, arcade, and 32-bit systems`. §5 PS1 row extended with PAL (`scph5502.bin`) and JP (`scph5500.bin`). §6 8BitDo Pro 2 / Ultimate pairing `D/A` → `macOS mode (M)`. §9 Mupen64Plus-Next pin list: `video_threaded=false` ([#14978](https://github.com/libretro/RetroArch/issues/14978)) added (row previously listed 2 of 3 frontend pins). §9 PCSX-ReARMed: `Run Ahead + preemptive inherit off` → `Run Ahead pinned "false" (interpreter safety); preemptive frames inherit off` (cfg pins `run_ahead_enabled`, only preemptive is inherited). §12 + §13: CHANGELOG style `kernel.org date<TAB>name` → `GNU ChangeLog style (YYYY-MM-DD<SP><SP>Author Name)` — actual file separator is double space. Version badge 2.80 → 2.81. Companion `retroarch-configs` bumped to v1.43.
