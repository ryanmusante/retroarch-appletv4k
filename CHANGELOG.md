2026-04-19  Ryan Musante

- v2.90: 1 `retroarch.cfg` value change. **Modifies:** `savestate_auto_index` rotation cap `savestate_max_keep = "5"` → `savestate_max_keep = "10"` (doubles the auto-indexed slot ring; still well inside purgeable-cache headroom on the 64 GB model, each compressed state is sub-MB for 8/16-bit cores and single-digit MB for N64/PS1, so the upper bound of 10 × largest-core-state is ~tens of MB). Key count unchanged at 75. `README.md` §7 Hotkeys save state behavior note — "Up to 5 auto-indexed state slots" → "Up to 10"; `savestate_max_keep` value reference updated. §7 Additional settings Saving cluster — `Auto-Index States` row Notes updated (`savestate_max_keep` reference) and `Max Auto-Increment States` row Value column 5 → 10. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.85 block removed). Version badge 2.89 → 2.90. Companion `retroarch-configs` bumped to v1.52 (SYNC — no `.cfg` / `.opt` changes).

2026-04-19  Ryan Musante

- v2.89: 2 `retroarch.cfg` adds. **Adds:** `savestate_auto_save = "true"` (captures in-memory state on Close Content; upstream default `false`). `savestate_auto_load = "false"` (explicit pin on upstream default; prevents auto-restore over current SRAM on content launch). Placed in the savestate cluster adjacent to `savestate_auto_index` and `savestate_max_keep`. Key count 73 → 75. `README.md` §7 Hotkeys warning — rewritten from prior "not auto-captured" caution to an informational note describing the new auto-save-on-exit + no-auto-load-on-launch behavior, the manual-load path (Select + L1), and the `savestate_max_keep = "5"` slot rotation. §7 Additional settings Saving cluster — two new rows: `Auto-Save on Exit` (`savestate_auto_save`) and `Auto-Load on Launch` (`savestate_auto_load`), placed directly after `Auto-Index States` so the three `savestate_auto_*` keys are visually grouped. Save/load hotkeys (Select + R1 / Select + L1) retained unchanged; Close Content hotkey (Select + Start) retained unchanged — the auto-save fires on that trigger. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.84 block removed). Version badge 2.88 → 2.89. Companion `retroarch-configs` bumped to v1.51 (SYNC — no `.cfg` / `.opt` changes).

2026-04-19  Ryan Musante

- v2.88: 4 `retroarch.cfg` changes + README trim. **Modifies:** `retroarch.cfg` L15 global shader swap `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"` → `"../shaders/shaders_slang/crt/crt-easymode.slangp"` (crt-easymode is single-pass, runs native 4K60 on A15, has no shader-controlled geometry so integer-scale is safe on every core including Tier 2 Mupen/PCSX, and produces a cleaner 4K phosphor mask than crt-aperture; crt-aperture retained as an option in §8 table). **Adds:** `video_vsync = "true"` (master vsync gate; paired with existing `video_swap_interval` and `video_max_swapchain_images` pins; upstream default but explicit to match the netplay/cloud_sync hardening pattern). `sort_savefiles_enable = "true"` (upstream default, pinned against drift; prior documentation already relied on per-core save sorting). `sort_savestates_enable = "true"` (same rationale). Key count 70 → 73 (+3 adds, L15 value swap). **README updates — intro and §3/§4 path phrasing:** L11 intro — dropped the "File Browser and System/BIOS directory paths are set to relative `../ROMs` and `../BIOS`" clause; replaced with a pointer to §4 configure-in-app. §3 Recommended setup L125–L126 — dropped the "The companion `retroarch.cfg` sets **File Browser**/**System/BIOS**" lines; replaced with explicit Settings → Directory instructions (ROM browser and System/BIOS paths are set per-device in-app, not by the companion cfg). §4 Saves/states note — rewrote: now documents the new `sort_savefiles_enable` / `sort_savestates_enable` pins and states that save/state directory locations are configured in Settings → Directory. §8 Shaders path note — trimmed the parenthetical cross-ref to the filesystem-layout section. **README updates — shader swap:** §7 Video settings Shader Preset row — crt-aperture → crt-easymode; Notes reworded to add "no shader geometry (integer-scale safe)". §8 Applying a shader intro — rewritten: "As of v2.84" → "As of v2.88"; rationale for crt-easymode over crt-aperture; Handheld note updated. §8 tuning paragraph (L407) — parameter list corrected to real crt-easymode slang parameters (SHARPNESS_IMAGE, SHARPNESS_EDGES, GLOW_*, MASK_COLORS, MASK_STRENGTH, MASK_SIZE, SCANLINE_SIZE_MIN/MAX, SCANLINE_SHAPE, GAMMA_INPUT/OUTPUT, BRIGHTNESS). §8 Recommended presets table — `crt-easymode.slangp` row gets **Global default** marker; `crt-aperture.slangp` row demoted to a regular entry with aperture-grille positioning. §8 Global default paragraph — crt-easymode instead of crt-aperture. §11 Setup Checklist Verify-after-relaunch — "crt-aperture" → "crt-easymode". **README additions (for new cfg pins):** §7 Video settings — new `VSync` row after Swap Interval. §7 Additional settings Saving cluster — new `Sort Save Files` and `Sort Save States` rows. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.83 block removed). Version badge 2.87 → 2.88. Companion `retroarch-configs` bumped to v1.50 (documentation sync: §5 Shaders `crt-aperture` → `crt-easymode`; no `.cfg` / `.opt` changes).

2026-04-19  Ryan Musante

- v2.87: Documentation corrections — no `retroarch.cfg` changes. (1) `retroarch.cfg` key-count arithmetic correction — actual non-blank, non-comment key count is 70, not 75 as previously stated. v2.85 entry key-count delta corrected from "Key count 71 → 75 (−1 + 5)" to "Key count 66 → 70 (−1 + 5)"; v2.84 entry delta corrected from "Key count 70 → 71" to "Key count 65 → 66"; v2.86 "All 75 `retroarch.cfg` keys" statement corrected to "All 70 `retroarch.cfg` keys". Individual `retroarch.cfg` keys, values, adds and removes from v2.84 and v2.85 are unchanged — only the running arithmetic was wrong (error originated in the trimmed v2.82 entry's "All 70 keys now documented" claim, which miscounted; actual was 65). (2) §7 Video settings Integer Scale row (L283) — Notes column expanded to clarify that global `video_scale_integer` (bool) is orthogonal to per-core `video_scale_integer_scaling` (mode, next row); Mupen64Plus-Next inherits the global bool without a per-core overscale mode (plain integer scale at native 320×240). Previous Notes ("Pixel-perfect output; produces borders at 4K") retained; clarification added alongside. All 70 `retroarch.cfg` keys and values unchanged from v2.86. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.82 block removed). Version badge 2.86 → 2.87. Companion `retroarch-configs` bumped to v1.49 (documentation corrections: §4 `video_scale_integer` row reworded to clarify global/per-core drift-guard relationship and Mupen inheritance; intro phrasing relaxed; v1.46/v1.47 CHANGELOG companion key-count references corrected to match; descriptive header comments prepended to all 16 `.cfg` / `.opt` files).

2026-04-18  Ryan Musante

- v2.86: Documentation corrections — no `retroarch.cfg` changes. (1) §7 Video settings Integer Overscale row (L283) — appended PCSX-ReARMed to the per-core enumeration ("256–640 variable; borders may shift on mode switch"); companion `retroarch-configs` README §4 already listed "All Tier 1 + PCSX-ReARMed" and PCSX.cfg L5–L6 set `video_scale_integer = "true"` + `video_scale_integer_scaling = "1"`; cross-repo doc desync resolved. (2) §8 Recommended presets table (L394) — `crt-aperture.slangp` Best For column reframed from "SNES, Genesis (lighter than crt-hyllian)" to "**Global default** (all cores); best all-rounder across 2D systems, lighter than crt-hyllian" to reflect v2.84 architecture reversal. (3) §8 guidance paragraph (L399) — rewritten to reconcile with v2.84: `crt-aperture` applies to all cores including Tier 2 Mupen/PCSX by default; Minimal presets (`zfast_crt`, `crt-pi`, `crt-potato-warm/cool`) are now the per-core downgrade path via Save Core Preset when Tier 2 cores drop frames, not the default guidance. (4) §7 Latency reduction Run Ahead row — clarified that global `run_ahead_frames = "1"` is the fallback for cores without a per-core `run_ahead_frames` override (inert unless Run Ahead is toggled on per-core). (5) §11 Setup Checklist Verify-after-relaunch — added `cloud_sync_driver` empty-string verify item (cfg-merge tools that treat empty strings as unset can silently re-introduce the upstream driver default). (6) §13 Versioning — added explicit statement of the CHANGELOG 5-MINOR-version trim policy that prior entries had invoked but not documented. All 70 `retroarch.cfg` keys, values, and key count unchanged from v2.85. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.81 block removed). Version badge 2.85 → 2.86. Companion `retroarch-configs` bumped to v1.48 (documentation corrections: A15 core topology corrected to binned 2P+3E for ATV4K 3rd Gen; §11 trim policy documented; v1.47 `mupen64plus-angrylion-multithread` rationale corrected; v1.46 "6 cores" counting error fixed).

