2026-04-19  Ryan Musante

- v2.87: Documentation corrections — no `retroarch.cfg` changes. (1) `retroarch.cfg` key-count arithmetic correction — actual non-blank, non-comment key count is 70, not 75 as previously stated. v2.85 entry key-count delta corrected from "Key count 71 → 75 (−1 + 5)" to "Key count 66 → 70 (−1 + 5)"; v2.84 entry delta corrected from "Key count 70 → 71" to "Key count 65 → 66"; v2.86 "All 75 `retroarch.cfg` keys" statement corrected to "All 70 `retroarch.cfg` keys". Individual `retroarch.cfg` keys, values, adds and removes from v2.84 and v2.85 are unchanged — only the running arithmetic was wrong (error originated in the trimmed v2.82 entry's "All 70 keys now documented" claim, which miscounted; actual was 65). (2) §7 Video settings Integer Scale row (L283) — Notes column expanded to clarify that global `video_scale_integer` (bool) is orthogonal to per-core `video_scale_integer_scaling` (mode, next row); Mupen64Plus-Next inherits the global bool without a per-core overscale mode (plain integer scale at native 320×240). Previous Notes ("Pixel-perfect output; produces borders at 4K") retained; clarification added alongside. All 70 `retroarch.cfg` keys and values unchanged from v2.86. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.82 block removed). Version badge 2.86 → 2.87. Companion `retroarch-configs` bumped to v1.49 (documentation corrections: §4 `video_scale_integer` row reworded to clarify global/per-core drift-guard relationship and Mupen inheritance; intro phrasing relaxed; v1.46/v1.47 CHANGELOG companion key-count references corrected to match; descriptive header comments prepended to all 16 `.cfg` / `.opt` files).

2026-04-18  Ryan Musante

- v2.86: Documentation corrections — no `retroarch.cfg` changes. (1) §7 Video settings Integer Overscale row (L283) — appended PCSX-ReARMed to the per-core enumeration ("256–640 variable; borders may shift on mode switch"); companion `retroarch-configs` README §4 already listed "All Tier 1 + PCSX-ReARMed" and PCSX.cfg L5–L6 set `video_scale_integer = "true"` + `video_scale_integer_scaling = "1"`; cross-repo doc desync resolved. (2) §8 Recommended presets table (L394) — `crt-aperture.slangp` Best For column reframed from "SNES, Genesis (lighter than crt-hyllian)" to "**Global default** (all cores); best all-rounder across 2D systems, lighter than crt-hyllian" to reflect v2.84 architecture reversal. (3) §8 guidance paragraph (L399) — rewritten to reconcile with v2.84: `crt-aperture` applies to all cores including Tier 2 Mupen/PCSX by default; Minimal presets (`zfast_crt`, `crt-pi`, `crt-potato-warm/cool`) are now the per-core downgrade path via Save Core Preset when Tier 2 cores drop frames, not the default guidance. (4) §7 Latency reduction Run Ahead row — clarified that global `run_ahead_frames = "1"` is the fallback for cores without a per-core `run_ahead_frames` override (inert unless Run Ahead is toggled on per-core). (5) §11 Setup Checklist Verify-after-relaunch — added `cloud_sync_driver` empty-string verify item (cfg-merge tools that treat empty strings as unset can silently re-introduce the upstream driver default). (6) §13 Versioning — added explicit statement of the CHANGELOG 5-MINOR-version trim policy that prior entries had invoked but not documented. All 70 `retroarch.cfg` keys, values, and key count unchanged from v2.85. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.81 block removed). Version badge 2.85 → 2.86. Companion `retroarch-configs` bumped to v1.48 (documentation corrections: A15 core topology corrected to binned 2P+3E for ATV4K 3rd Gen; §11 trim policy documented; v1.47 `mupen64plus-angrylion-multithread` rationale corrected; v1.46 "6 cores" counting error fixed).

2026-04-18  Ryan Musante

- v2.85: 6 corrections to `retroarch.cfg`. **Adds:** `audio_driver = "coreaudio"` (pins driver against silent fallback; `coreaudio3` is master-only per [RA] CHANGES.md and must not be used in v1.22.x stable). `netplay_nat_traversal = "false"` (registration-site default is `true` at [RA] configuration.c:2272 — the `DEFAULT_NETPLAY_NAT_TRAVERSAL=false` macro at config.def.h:1371 is dead code, so without this pin a fresh config performs UPnP/NAT-PMP probes on startup). `input_overlay_enable = "false"` (v1.21.0 changed preferred-overlay auto-load default on mobile-class platforms, triggering an overlay subsystem scan at startup on tvOS where no touch surface exists). `netplay_use_mitm_server = "false"` (matches existing explicit hardening pattern — six `cloud_sync_*` keys already pinned default-false — guarding against relay-server opt-in on default drift). `netplay_start_as_spectator = "false"` (closes remaining netplay attack-surface keys). **Removes:** `video_use_metal_arg_buffers = "true"` stripped (key registration guarded by `#if defined(__APPLE__) && defined(HAVE_VULKAN)` at [RA] configuration.c:1970–1972, inert when `video_driver=metal`; re-activates only if video_driver is switched to vulkan). Key count 66 → 70 (−1 + 5). `README.md` §7 Video settings: `Metal Argument Buffers` row removed. §7 Additional settings: added `Audio | Audio Driver` row (above `audio_out_rate`); added `Input | Overlay Subsystem` row (after `Menu Toggle Combo`); added three `Network` rows for `netplay_nat_traversal`, `netplay_use_mitm_server`, `netplay_start_as_spectator` (between existing `Netplay Public Announce` and `Discord RPC`). CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.80 block removed). Version badge 2.84 → 2.85. Companion `retroarch-configs` bumped to v1.47 (11 corrections across 8 `.cfg` and 3 `.opt` files).

2026-04-17  Ryan Musante

- v2.84: Shader architecture reversal — reinstate global `video_shader` in `retroarch.cfg`; companion `retroarch-configs` removes per-core `video_shader` lines. `retroarch.cfg` L15 added: `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"` (single-pass; runs native 4K60 on A15; brighter than crt-easymode; Low GPU cost per §8 table). Selected as best all-rounder across Tier 1 CRT cores and Tier 2 interpreter cores. Key count 65 → 66. `README.md` §7 Video settings: Video Shaders row Notes simplified to pipeline gate only; new `Shader Preset` row added documenting the global `video_shader` path. §8 Applying a shader: rewritten to describe global default + per-core override workflow (reverses v2.79 per-core-only architecture). §8 Handheld note added for mGBA users who prefer LCD aesthetic (apply `handheld/lcd-grid-v2.slangp` via Save Core Preset). §8 tuning paragraph: `crt-easymode 4K parameters` → `crt-aperture 4K parameters` with generic "tune in Shader Parameters menu" guidance (no invented values). §8 L403 Note: stale per-core shader path explanation simplified (no longer references per-core `config/<core_name>/` paths for shaders; still resolves same way as `../ROMs`, `../BIOS`, `../saves`). §11 Setup Checklist shader-verify item: per-core list → global `crt-aperture`. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.79 block removed). Version badge 2.83 → 2.84. Companion `retroarch-configs` bumped to v1.46 (per-core `video_shader` stripped from all 8 `.cfg` files; per-core `.cfg` key counts all decrease by 1).

2026-04-17  Ryan Musante

- v2.83: SYNC — no `retroarch.cfg` changes. CHANGELOG trimmed to last 5 minor versions per §13 policy (v2.77 + v2.78 blocks removed). Version badge 2.82 → 2.83. Companion `retroarch-configs` bumped to v1.45: removed dead sentinel `config/.gitkeep` (redundant with 16 tracked `.cfg`/`.opt` files); `README.md` §2 File Structure line updated from "8 `.cfg`, 8 `.opt`, and `config/.gitkeep`" → "8 `.cfg` and 8 `.opt` files, flat under `config/`"; companion CHANGELOG trimmed to last 5 minor versions (v1.39 block removed). Companion shipped ZIP now contains exactly 16 files under `config/` (was 17). No per-core override changes.
